# 密码生成器网站 PRD

| 字段 | 内容 |
| --- | --- |
| 产品名称 | Password Master（工作目录：password-mster1） |
| 文档版本 | v1.0 |
| 状态 | 草稿 |
| 负责人 | 林（Product Manager） |
| 更新日期 | 2026-09-19 |

---

## 1. 背景与问题

用户日常需要为账号创建高强度、互不重复的密码，但常见做法存在明显问题：

- 自己编密码：规律弱、易被猜、跨站复用。
- 浏览器自带生成器：与特定浏览器绑定，规则不够透明，不便分享给团队或家人说明「怎么生成的」。
- 在线生成器：不少产品会把密码发到服务端，用户对隐私不放心。
- 密码过长、含特殊字符时，部分网站登录规则冲突，用户需要快速调整规则再生成。

本产品要做的是：**一个可在浏览器本地完成、规则清晰、可一键复制的密码生成网站**，让「生成强密码」这件事在 10 秒内完成，且用户能确信密码没有离开自己的设备。

---

## 2. 产品目标

### 2.1 北极星指标

**7 日内至少成功复制 1 次密码的独立访问用户数（Weekly Copiers）。**

复制是真实使用信号，比「打开页面」更能说明产品创造了价值。

### 2.2 业务目标（V1）

| 目标 | 指标 | 目标值（上线 4 周后） |
| --- | --- | --- |
| 用户能快速拿到可用密码 | 从进入首页到首次复制的中位时长 | ≤ 15 秒 |
| 用户信任本地生成 | 页面明确展示「仅在本地生成」且无服务端落库 | 100% 满足 |
| 生成结果可用 | 按默认规则生成后，用户未改规则直接复制的比例 | ≥ 60% |
| 规则可调 | 调整长度或字符集后再次生成并复制的比例 | ≥ 25% |

### 2.3 非目标（本版本不做）

- 不做账号体系、云端保险库、跨设备同步。
- 不做密码填写插件 / 浏览器扩展（可列为后续）。
- 不做「检测某网站密码是否泄露」类功能（依赖第三方数据，合规与成本高）。
- 不收集、不上报、不存储用户生成的明文密码。

---

## 3. 目标用户

| 画像 | 场景 | 核心诉求 |
| --- | --- | --- |
| 个人用户 | 注册新网站、改密、临时生成一次性口令 | 快、强、能复制、不用注册 |
| 注重隐私的用户 | 不信任「在线工具会不会记我密码」 | 本地生成、可解释、无账号 |
| 轻度办公用户 | 给测试账号、临时共享环境生成口令 | 可调长度/字符集，避免目标系统拒收 |

V1 以个人用户为主；B 端团队密码策略（强制复杂度、批量导出）放到后续。

---

## 4. 产品原则

1. **密码不出域**：生成、强度评估、复制均在浏览器完成；服务端只托管静态页面与匿名统计（若有）。
2. **默认即安全**：首次打开即可用安全默认值生成，不必先理解所有选项。
3. **规则可见**：用户能看见长度、字符类型、是否排除易混淆字符。
4. **一次一事**：首页只服务「生成 → 看一眼强度 → 复制」；设置不淹没主操作。
5. **失败可恢复**：目标网站不接受某类字符时，用户能关掉该类并立刻重生成。

---

## 5. 用户故事与验收标准

### 5.1 默认生成并复制

**作为**访问者，**我希望**打开网站就能看到一条强密码并一键复制，**以便**立刻用在注册或改密流程里。

验收：

- 首次进入即展示一条按默认规则生成的密码（不必先点「生成」）。
- 提供「复制」主按钮；复制成功有明确反馈（如「已复制」），失败时提示手动选择复制。
- 提供「重新生成」，每次结果应不同（在密码空间足够大的前提下）。

### 5.2 调整长度与字符集

**作为**用户，**我希望**调整长度并开关大小写、数字、符号，**以便**满足不同网站的密码规则。

验收：

- 长度可调，范围建议 **8–64**，默认 **16**。
- 字符类型：小写、大写、数字、符号，至少选一类；若全部关闭，禁止生成并提示原因。
- 选项变更后自动重新生成（或提供明确「应用并生成」，二选一，V1 建议自动生成以减少点击）。

### 5.3 排除易混淆字符

**作为**需要手抄或对着屏幕输入的用户，**我希望**排除 `0/O`、`1/l/I` 等易混字符，**以便**减少输错。

验收：

- 提供「排除易混淆字符」开关，默认开启。
- 排除集合需在文档或页面说明中列出（建议：`0 O o 1 l I |` 等，最终以实现清单为准）。

### 5.4 密码强度提示

**作为**用户，**我希望**看到当前密码的强度，**以便**判断是否够用。

验收：

- 展示可读等级（如：弱 / 中 / 强 / 极强）及简要原因（长度不足、字符种类少等）。
- 强度仅基于当前生成结果与所选规则，不把密码发到服务端。
- 弱密码时仍允许复制（用户可能受目标网站长度限制），但视觉上提示风险。

### 5.5 显示 / 隐藏密码

**作为**用户，**我希望**可以短暂查看明文或打码显示，**以便**在公共场合降低肩窥风险。

验收：

- 默认显示明文（生成器场景需要确认内容）；提供「隐藏」切换。
- 隐藏后复制功能仍可用。

### 5.6 隐私说明可达

**作为**注重隐私的用户，**我希望**在页面上立刻看到「密码仅在本地生成」，**以便**决定是否使用。

验收：

- 首页可见一句隐私承诺，并可点到简短说明（生成算法在前端、无账号、无密码日志）。
- 不在 URL、标题、分析事件中带上生成出的密码明文。

---

## 6. 功能范围

### 6.1 V1 必须有（P0）

| ID | 功能 | 说明 |
| --- | --- | --- |
| F1 | 随机密码生成 | 使用浏览器安全随机源（如 `crypto.getRandomValues`），禁止 `Math.random` 作为唯一熵源 |
| F2 | 默认规则 | 长度 16；大小写 + 数字 + 符号全开；排除易混淆字符默认开 |
| F3 | 规则面板 | 长度滑块/输入；四类字符开关；易混淆开关 |
| F4 | 一键复制 | Clipboard API；失败降级提示 |
| F5 | 重新生成 | 主按钮，键盘可操作 |
| F6 | 强度展示 | 本地计算，等级 + 一句解释 |
| F7 | 显示/隐藏 | 切换明文 |
| F8 | 隐私文案 | 首页承诺 + 简短说明页或抽屉 |
| F9 | 响应式布局 | 桌面与手机均可单手完成「生成 + 复制」 |

### 6.2 V1 应该有（P1）

| ID | 功能 | 说明 |
| --- | --- | --- |
| F10 | 符号集可预览 | 展示实际会用到的符号，避免用户不知道「符号」包含哪些 |
| F11 | 最近一次规则记忆 | 使用 `localStorage` 仅保存规则，不保存密码明文 |
| F12 | 无障碍 | 按钮有明确名称；强度不仅靠颜色；支持键盘 Tab / Enter |

### 6.3 后续版本（P2，不纳入 V1 排期承诺）

- 口令短语（diceware / 多词空格分隔）。
- 批量生成 N 条并导出（仅本地下载）。
- PIN / 纯数字模式快捷预设。
- 「类似网站规则」预设（如「仅字母数字、最长 20」）。
- PWA 离线可用。
- 浏览器扩展、密码库。

---

## 7. 信息架构与关键页面

V1 建议单页应用 + 页脚说明，降低跳转成本。

```
首页 /
  ├ 密码展示区（明文/掩码、复制、重新生成）
  ├ 强度
  ├ 规则（长度、字符类型、易混淆）
  └ 页脚：隐私说明、关于

/privacy 或首页内嵌面板
  └ 本地生成说明、不收集清单
```

不需要注册、登录、设置中心。

---

## 8. 交互与体验要点

### 8.1 首屏

- 密码是视觉焦点，字号大于正文。
- 主按钮「复制」比「重新生成」更突出。
- 规则折叠：桌面默认可全露出；移动端规则可在首屏下方，避免挡住复制。

### 8.2 复制反馈

- 成功：按钮文案变为「已复制」，约 2 秒后恢复。
- 失败：提示「复制失败，请长按密码手动复制」（移动端常见）。

### 8.3 非法规则

- 长度为 0、非数字、低于下限：回退到最近合法值或默认 16。
- 字符类型全关：禁用生成与复制，文案：「请至少选择一种字符」。

### 8.4 空状态 / 加载

- 静态站点，无列表空状态。
- 若脚本未加载：展示不依赖 JS 的说明「请启用 JavaScript 以本地生成密码」，避免空白页被当成故障。

---

## 9. 安全与隐私要求（硬约束）

本产品的信任建立在「我们看不到你的密码」上，以下为发布门槛：

1. **纯前端生成**：生成逻辑不得依赖后端接口返回密码。
2. **安全随机数**：必须使用 CSPRNG（Web Crypto）。
3. **不落日志**：分析埋点只允许匿名事件（如 `copy_click`、`regenerate_click`），事件属性不得包含密码、规则组合可匿名聚合。
4. **不写 URL**：禁止 `?password=` 一类可分享明文。
5. **本地存储**：只允许存用户偏好（长度、开关），禁止存生成历史明文（V1）。
6. **依赖最小化**：第三方脚本需评估是否会读取 DOM 中的密码；V1 尽量零追踪或仅用无 DOM 扫描的匿名统计。
7. **HTTPS**：生产环境强制 HTTPS。

---

## 10. 技术约束（给研发的边界，非实现方案）

- 静态站点即可（任意现代前端栈），无强制后端。
- 兼容最近两个版本的 Chrome、Edge、Safari、Firefox；iOS Safari 需验证复制降级。
- 中文界面为 V1 默认语言；文案简体中文。
- 不引入需把用户数据送出浏览器的账号/云 SDK。

---

## 11. 成功度量与埋点（匿名）

允许的事件示例：

| 事件 | 用途 |
| --- | --- |
| `page_view` | 流量 |
| `copy_success` / `copy_fail` | 北极星与复制稳定性 |
| `regenerate` | 是否对默认结果不满意 |
| `option_change` | 哪类规则最常被改（只记选项名，不记密码） |

看板：

- 访问 → 复制转化率
- 复制失败率（按设备类型）
- 默认规则直接复制 vs 改规则后复制

---

## 13. 开放问题

1. 品牌名最终用 Password Master 还是更短的中文名？
2. V1 是否需要英文界面？
3. 符号默认集是否排除空格及高冲突符号（如 `'` `"` `\`）？建议默认排除空格与引号，降低表单/SQL 类站点报错。
4. 是否在页脚放「算法说明」（charset + 均匀随机）以增强信任？

---

## 14. 附录：默认规则草案

| 项 | 默认值 |
| --- | --- |
| 长度 | 16 |
| 小写 a–z | 开 |
| 大写 A–Z | 开 |
| 数字 0–9 | 开 |
| 符号 | 开（集合待实现时锁定，建议避开空格与引号） |
| 排除易混淆 | 开 |
| 生成后显示 | 明文 |
| 记住规则 | 开（仅本地） |

---

*本文档描述「要做成什么样」和「怎样算做对」，不绑定具体框架与视觉稿。实现阶段若与本文冲突，以安全与隐私硬约束优先，功能增减需更新版本号。*
